%nav
  %ul.pager
    %li.previous
      = link_to '&larr; Previous'.html_safe, '#', 'ng-click': 'previousPage(); $event.preventDefault();'
    %li.next
      = link_to 'Next &rarr;'.html_safe, '#', 'ng-click': 'nextPage(); $event.preventDefault();'
